# Optional production-only settings. Local development needs neither value.

# Protect a raw origin behind a reverse proxy that injects X-Gate-Secret.
GARDA_GATE_SECRET=

# Enable cookieless Cloudflare Web Analytics in rendered pages.
CF_BEACON_TOKEN=
